@model GolfCentraAdmin.ViewModel.EmployeeViewModel
@{ Layout = "~/Views/Shared/_PartialLayoutPage.cshtml";}
<div class="modal-dialog modal-lg">
    <div class="modal-content popup_adj">
        <div class="modal-header" style="padding-bottom:35px">
            <h4 class="modal-title" id="myLargeModalLabel">Employee Details</h4>
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true" onclick="CloseModalPopup('divTimePopUp')" >×</button>
        </div>
        <div class="modal-body">
            <div class="row">
                @using (Ajax.BeginForm(new AjaxOptions { OnBegin = "OnEmployeeEditBegin", OnSuccess = "OnEmployeeEditSuccess", Url = @GolfCentraAdmin.UI.Common.Constants.Url.WebSiteUrl + "EmployeeManagement/UpdateEmployee" }))
                {
                    <div class="col-md-12">
                        <div class="card card-body">
                            @Html.HiddenFor(x => x.EmployeeId)
                            <div class="row">
                                <div class="col-sm-12 col-xs-12">
                                    <form>
                                        <div class="form-group">
                                            <label for="exampleInputEmail1">Name</label>
                                            @Html.TextBoxFor(x => x.Name, new { @id = "txtNamePopUp", @autocomplete = "off", @class = "form-control", @placeholder = "Name" })

                                        </div>
                                        <div class="form-group">
                                            <label for="exampleInputEmail1">Email ID</label>
                                            @Html.TextBoxFor(x => x.EmailId, new { @id = "txtEmailPopUp", @autocomplete = "off", @class = "form-control", @placeholder = "Email" })
                                        </div>

                                        <div class="form-group">
                                            <label for="Mobilenumber">Mobile Number</label>
                                            @Html.TextBoxFor(x => x.Mobile, new { @id = "txtMobileNumberPopUp", @autocomplete = "off", @class = "form-control", @placeholder = "MobileNumber" })
                                        </div>
                                          <div class="form-group">
                                            <label for="Mobilenumber">Password</label>
                                            @Html.TextBoxFor(x => x.Password, new { @id = "txtPassword", @autocomplete = "off", @class = "form-control", @placeholder = "Password" })
                                        </div>

                                        <button type="submit" class="btn btn-success waves-effect waves-light m-r-10" id="btnSubmitPopUp">Submit</button>
                                        <div id="divMessagePopUp"></div>
                                        <div id="divLoaderPopUp" style="display:none">
                                            <img src="@GolfCentraAdmin.UI.Common.Constants.Url.WebSiteUrlWithoutSlash/Theme/images/loader.svg" />
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>

        </div>

    </div>
    <!-- /.modal-content -->
</div>
<!-- /.modal-dialog -->
